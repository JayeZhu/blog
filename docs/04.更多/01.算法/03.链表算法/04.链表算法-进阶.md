---
title: 链表算法-进阶
date: 2025-10-23 15:24:24
permalink: /pages/c23ccd/
categories:
  - 更多
tags:
  - 算法
  - 链表算法
---
## 部分反转
### ​92. 反转链表 II​：反转链表中指定区间内的节点。
```javascript
var reverseBetween = function(head, left, right) {
    let dummy = new ListNode(-1);
    dummy.next = head;
    let pre = dummy;
    
    for (let i = 0; i < left - 1; i++) {
        pre = pre.next;
    }
    
    let cur = pre.next;
    for (let i = 0; i < right - left; i++) {
        const next = cur.next;
        cur.next = next.next;
        next.next = pre.next;
        pre.next = next;
    }
    return dummy.next;
};
```

## 链表合并
### ​21. 合并两个有序链表​：将两个有序链表合并成一个新的有序链表。

```javascript
var mergeTwoLists = function(l1, l2) {
    let dummy = new ListNode(-1);
    let cur = dummy;
    
    while (l1 && l2) {
        if (l1.val <= l2.val) {
            cur.next = l1;
            l1 = l1.next;
        } else {
            cur.next = l2;
            l2 = l2.next;
        }
        cur = cur.next;
    }
    
    cur.next = l1 || l2;
    return dummy.next;
};
```

## 回文判断
### ​234. 回文链表​：判断链表是否为回文结构。
```javascript
var isPalindrome = function(head) {
    // 找到中间节点
    let slow = head, fast = head;
    while (fast && fast.next) {
        slow = slow.next;
        fast = fast.next.next;
    }
    
    // 反转后半部分
    let prev = null, cur = slow;
    while (cur) {
        const next = cur.next;
        cur.next = prev;
        prev = cur;
        cur = next;
    }
    
    // 比较前后两部分
    let p1 = head, p2 = prev;
    while (p2) {
        if (p1.val !== p2.val) return false;
        p1 = p1.next;
        p2 = p2.next;
    }
    return true;
};
```